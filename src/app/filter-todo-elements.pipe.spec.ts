import { FilterTodoElementsPipe } from './filter-todo-elements.pipe';

describe('FilterTodoElementsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTodoElementsPipe();
    expect(pipe).toBeTruthy();
  });
});
